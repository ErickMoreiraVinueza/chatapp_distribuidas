# Variables de entorno para Docker Compose
# Copia este archivo a .env en la raíz del proyecto

# JWT Secret (cambiar en producción)
JWT_SECRET=tu_clave_secreta_super_segura_para_docker_2024

# MongoDB (no cambiar si usas docker-compose)
MONGODB_URI=mongodb://mongodb:27017/chatapp

# Puertos (cambiar si están ocupados)
MONGODB_PORT=27017
BACKEND_PORT=5000
FRONTEND_PORT=5173
